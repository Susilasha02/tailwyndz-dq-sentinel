@"
# Python / editor / OS
__pycache__/
*.py[cod]
.venv/
.vscode/
.DS_Store
Thumbs.db

# Ignore raw data by default
data/

# But explicitly allow small cleaned datasets and key lookup files used in CI
!data/cleaned/
!data/cleaned/*
!data/calendar.csv
!data/promos.csv

# Reports produced by CI (allow them if present locally)
!reports/
!reports/*
"@ > .gitignore