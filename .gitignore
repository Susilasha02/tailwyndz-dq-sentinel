@"
# Python / editor / OS
__pycache__/
*.py[cod]
.venv/
.vscode/
.DS_Store
Thumbs.db

# Ignore data root by default (do not commit large raw datasets)
data/

# But explicitly allow small cleaned datasets and key lookup files that we need for CI
!data/cleaned/
!data/cleaned/*
!data/calendar.csv
!data/promos.csv

# Reports produced by CI (allow them if present locally)
!reports/
!reports/*
"@ > .gitignore