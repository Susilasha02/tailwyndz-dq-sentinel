@"
# Python / editor / OS
__pycache__/
*.py[cod]
.venv/
.vscode/
.DS_Store
Thumbs.db

# Ignore raw data by default (won't commit huge raw datasets)
data/

# But explicitly allow cleaned data and the tiny lookup files we need for CI
!data/cleaned/
!data/cleaned/*
!data/calendar.csv
!data/promos.csv

# Reports produced by CI (allow them if present locally)
!reports/
!reports/*
"@ > .gitignore